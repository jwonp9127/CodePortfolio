public enum ItemType
{
    Equipment,
    Consumable,
    Material
}

public enum Ability
{
    HP,
    SP,
    MoveSpeed,
    AttackPower,
    AttackSpeed,
    MaxGaugeTime
}

public enum NPCType
{
    Monster,
    Villager
}

public enum Category
{
    NPC = 0,
    Crop = 1000,
    Item = 2000
}